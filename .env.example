# NoneBot2 配置文件示例
# .env.prod

# NoneBot 基本配置
HOST=0.0.0.0
PORT=8080
LOG_LEVEL=INFO

# OneBot 配置
# 如果使用 go-cqhttp 或其他 OneBot 实现，需要配置连接地址
# ONEBOT_WS_URLS=["ws://127.0.0.1:6700"]

# 数据库配置（用于 chatrecorder）
# 默认使用 SQLite，也可以配置为 PostgreSQL 或 MySQL
# SQLALCHEMY_DATABASE_URL=sqlite+aiosqlite:///data.db
# SQLALCHEMY_DATABASE_URL=postgresql+asyncpg://user:password@localhost/dbname
# SQLALCHEMY_DATABASE_URL=mysql+aiomysql://user:password@localhost/dbname

# 插件配置
# 你可以在这里添加其他配置项
